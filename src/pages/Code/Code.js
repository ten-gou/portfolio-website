import { Typography } from "@mui/material"

const CodeList = () => {
    const list = [
        {
            'name': '',
        },

    ]
}

const Code = () => {
    return (
        <Typography>Code</Typography>
    )
}

export default Code;